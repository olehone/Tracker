@using Tracker.Domain.Enums
@using Tracker.WebApp.Components.BoardUsers
@inherits BoardUsersSubscribeBase

<MudSelect T=@UserBoardRole
		   Value=@_currentRole
		   ValueChanged=@(v => _currentRole = v)
		   Dense
		   Style="min-width: 160px; width: 160px;"
		   Margin=@Margin.None
		   Variant=@Variant.Text>

	<MudSelectItem Value=@UserBoardRole.Observer>
		<MudStack Row>
			<MudIcon Icon=@BoardRoleHelper.GetRoleIcon(UserBoardRole.Observer) />
			Observer
		</MudStack>
	</MudSelectItem>

	<MudSelectItem Value=@UserBoardRole.Member>
		<MudStack Row>
			<MudIcon Icon=@BoardRoleHelper.GetRoleIcon(UserBoardRole.Member) />
			Member
		</MudStack>
	</MudSelectItem>

	<MudSelectItem Value=@UserBoardRole.Admin>
		<MudStack Row>
			<MudIcon Icon=@BoardRoleHelper.GetRoleIcon(UserBoardRole.Admin) />
			Admin
		</MudStack>
	</MudSelectItem>

</MudSelect>

@if (HasChanged)
{
	<MudIconButton Icon=@Icons.Material.Filled.Check
				   Variant=@Variant.Outlined
				   Color=@Color.Success
				   Size=@Size.Medium
				   OnClick=@SubmitAsync />
}