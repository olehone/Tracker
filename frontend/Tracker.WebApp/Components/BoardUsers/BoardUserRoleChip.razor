@using Tracker.Domain.Enums
@using Tracker.WebApp.Components.Boards

<MudChip T=@BoardUserRole
		 Icon=@BoardRoleHelper.GetRoleIcon(Role)
		 Color=@BoardRoleHelper.GetRoleColor(Role)
		 Size="Size.Small">
	@Role.ToString()
</MudChip>
