@using Tracker.Domain.Enums
@inherits BoardUsersSubscribeBase

<MudMenu Icon=@Icons.Material.Filled.MoreVert
		 Size=@Size.Small
		 AriaLabel="Dangerous zone">

	<MudMenuItem Label="Transfer ownership"
				 Icon=@BoardRoleHelper.GetRoleIcon(BoardUserRole.Owner)
				 IconColor=@BoardRoleHelper.GetRoleColor(BoardUserRole.Owner)
				 OnClick=@(() => ShowTransferOwnershipDialog()) />

	<MudMenuItem Icon=@Icons.Material.Filled.Delete
				 IconColor=@Color.Error
				 Label="Remove"
				 OnClick=@(() => UsersState.RemoveAsync(BoardUser)) />

</MudMenu>


