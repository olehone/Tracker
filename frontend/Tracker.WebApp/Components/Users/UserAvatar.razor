@using Tracker.WebApp.Components.Shared

<MudAvatar Style=@ImageStyle
           Variant="Variant.Outlined"
           Size=@Size
           @onmouseenter=@(() => HandleHovering.InvokeAsync(true))
           @onmouseleave=@(() => HandleHovering.InvokeAsync(false))>
    @if (!string.IsNullOrEmpty(_avatarUrl) && !_avatarFailed)
    {
        <CustomImage Src=@_avatarUrl ObjectFit="ObjectFit.Cover" HandleError=@HandleImageError />
    }
    else
    {
        @FirstLetter()
    }
</MudAvatar>